[seedwright]
# quiet, info, or debug
log_level = info

[oracle]
# Copy this file to seedwright.ini and set these for your environment.
user = scott
password = tiger
host = oracle.example.test
port = 1521
service_name = FREEPDB1

[postgres]
# Copy this file to seedwright.ini and set these for your environment.
host = postgres.example.test
port = 5432
dbname = app
user = scott
password = tiger
schema = public

# Advanced: you can use one psycopg connection string instead of the fields above.
# dsn = postgresql://scott:tiger@postgres.example.test:5432/app
